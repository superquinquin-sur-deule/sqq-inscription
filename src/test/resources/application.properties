stripe.redirect-domain=http://localhost:8080
stripe.api-key=unused
stripe.part-sociale-price-id=unused

quarkus.security.users.embedded.users.admin=password

# Matrix - disabled by default for tests, enabled via test profile
matrix.enabled=false
matrix.homeserver-url=http://localhost:9999
matrix.access-token=test-token
matrix.room-id=!test:localhost

# REST Client for Matrix API
quarkus.rest-client.matrix-api.url=http://localhost:9999